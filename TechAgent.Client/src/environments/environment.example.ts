// Copy this file to environment.ts (dev) or environment.prod.ts (prod) and fill in real values.
// Both environment.ts and environment.prod.ts are gitignored — never commit them.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5073',
  syncfusionLicenseKey: 'YOUR_SYNCFUSION_KEY'
};
